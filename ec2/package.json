{
  "name": "equinox-lambda-deployment",
  "version": "1.0.0",
  "description": "Lambda function for triggering EC2 deployments via Systems Manager",
  "main": "lambda-handler.js",
  "dependencies": {
    "aws-sdk": "^2.1000.0"
  },
  "engines": {
    "node": "18.x"
  }
}
